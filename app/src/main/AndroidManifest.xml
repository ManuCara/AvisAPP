<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.avis">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Avis">
        <activity android:name=".TermsAndPrivacy"></activity>
        <activity android:name=".Contact_us" />
        <activity android:name=".Logout" />
        <activity android:name=".Settings" />
        <activity android:name=".chi_siamo" />
        <activity android:name=".where_we_are" />
        <activity android:name=".Vademecum" />
        <activity android:name=".blood_weather" />
        <activity android:name=".News" />
        <activity android:name=".Profile" />
        <activity android:name=".HomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>